all:
	g++ benchmark.cpp --std=c++11 -g -o benchmark
	g++ arquivo.cpp  -o arquivo
	g++ arquivo2.cpp  -o arquivo2
	g++ writer.cpp  -o writer
	g++ reader.cpp  -o reader
	
clean:
	rm benchmark -rf
	rm arquivo arquivo.txt -rf
	rm arquivo2 arquivo2.txt -rf
	rm writer reader desafio.txt -rf
	rm *~ -rf